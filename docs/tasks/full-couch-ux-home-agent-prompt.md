# Starter prompt — home Mango deployment and full couch UX acceptance

```text
Work from the home-Mac Mango clone on branch feat/native-experience; verify the
branch and do not switch. Pull the latest origin tip and read
docs/tasks/FULL_COUCH_UX_HOME_ACCEPTANCE_CODEX_SPEC.md top to bottom before doing
anything—it is the full contract and must include afef49e, 63c34da, and 539ebdb.
Deploy only through Mango's Git scripts, run the full Pi gates, then verify every
launcher, Search, Detail, P0 state, HUD/Streams, voice/companion, Settings,
Reliability, controller, and locked display-sleep flow with me watching the TV.
Ask for one to three observations at a time and adapt to each answer. Capture a
failure before changing anything; make the smallest principled home-Mac fix, add
a regression test, commit/push, Git-deploy, and have me recheck it. Never
rsync/scp, edit Pi source, delete runtime state, touch YouTube credentials/quota,
use pairing mode as normal reconnect, or fabricate a couch pass. Push the report
with every DEFERRED item and exact next action, then leave the TV On at 30m with
playback stopped and launcher/controller usable.
```
